
<b>+ Upload a new video</b><br><br>
<div class="group">
  <%= f.label :title %>
  <%= f.text_field :title %>
</div>

<div class="group">
  <%= f.label :description %>
  <%= f.text_field :description %>
</div>
<br>
<b>or</b>
<br><br>
Copy YouTube URL here<br><br>
<%= f.text_field :youtube_url %><br>

<%= f.hidden_field :user_id, value: current_user.id %>

<% if @video.new_record?%>
	<%= f.submit "Save", class: "btn btn-info" %>
<%else%>
	<%= f.submit "Save", class: "btn btn-info" %>
<%end%>

or <%= link_to "Cancel", videos_path %>
