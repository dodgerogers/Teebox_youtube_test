
<h2><%= @question.title %></h2>

<%= simple_format @question.body %>

Tags: <%= question_tags(@question) %>

<div class="form-actions">
	<%= link_to "Edit", edit_question_path(@question), class: "btn btn-primary" if @question.user %>
</div>