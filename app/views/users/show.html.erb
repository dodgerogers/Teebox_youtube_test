
	<%= image_tag(@user.picture) %><br><br>
	<%= @user.name %><br>
	<%= @user.email %><br>
	
	<%= @user.gender %><br>
	<%= @user.country %><br>

		<%= link_to "Edit Profile", edit_user_path(@user) %><br><br>
		
		<%= render partial: "videos/index", locals: { videos: @videos } %>
